extends skeleton
block title
  title 500: Server Error
block content
  h1 Server Error
  p Something went wrong. It wasn't your fault.
  if err
      pre.code.stack= err.stack
  else
    p We do not have any more information.
  a(href='/') Return to home page
